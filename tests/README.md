# These tests are ass

```bash
python -m pytest -q
```

Notes:

- Tests are written to run without Flow Launcher.
- They focus on regression-prone business logic and cache behavior.
- When adding a bug fix, prefer adding a small regression test next to the affected area.
